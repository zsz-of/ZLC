# 更新日志 (Changelog)

本文件记录 Z-LivePhoto-Converter 各版本的更新内容。历史安装包请前往 [Releases](https://github.com/zsz-of/Z-LivePhoto-Converter/releases) 下载。

---

## [3.1.9] - 2026-09-06

### 🐛 修复：蓝奏内置浏览器下载捕获回退到 3.1.0 验证过的方案（v3.1.8 真机回归仍卡页面）

- **根因**：3.1.2 起把下载直链判定从「URL 含 `/file/`」误改为「文件服务器 host 后缀（webgetstore/dmpdmp）+ 后缀名」。蓝奏下载按钮的 `href` 是 `developer2.lanrar.com/file/?<token>` 这种**分发 URL**（路径含 `/file/`，服务端对其 302 到 CDN），host 后缀匹配漏掉了它，只能等 302 到 CDN 直链或 `DownloadListener` 触发——这二者在部分机型上不可靠，导致点下载后仍卡在浏览器。
- **修复（回到 3.1.0 实测可靠的逻辑）**：
  - 下载直链判定改回**宽泛匹配**：URL 含 `/file/` 或 `.zip/.apk/.rar/.7z/.exe` 后缀即命中，在按钮点击、新窗口首次导航到分发 URL 时就拦截（早于 302、不依赖 DownloadListener）；
  - 子 WebView（承接 `target=_blank` 弹窗）未命中下载时 `shouldOverrideUrlLoading` 返回 `true`（拦截非下载导航），避免弹窗逃逸到系统浏览器——与 3.1.0 行为一致；
  - 移除 v3.1.8 的「页面 JS 探针」与 JsBridge（回到 3.1.0 简洁方案，只依赖 WebView 导航拦截 + DownloadListener 兜底）。

---

## [3.1.8] - 2026-09-06

### 🐛 修复：蓝奏内置浏览器仍捕获不到下载（v3.1.7 真机回归：下载开始后不关页、无提示）

- **根因**：蓝奏分享页的下载按钮位于页内同域 iframe（`/fn?...` 解析页）中，点击是 `target=_blank` 新窗口 + 二级 302。Android WebView 对「iframe 内 target=_blank 弹窗」的承接（`onCreateWindow`/`DownloadListener`）在部分机型/内核上不触发，v3.1.7 恢复的多窗口机制仍漏捕获。
- **新方案（不再依赖弹窗/下载回调）**：实测还原蓝奏真实链路——分享页 iframe 的 XHR 请求 `ajaxfile.php` 后即渲染出「电信/联通/普通下载」三个按钮，其 `href` 直接就是 `developer2.lanrar.com/file/?<token>` 分发 URL，服务端对该 URL 直接 302 到带签名的 CDN 直链（无需 Cookie/Referer/JS）。
  - 页面加载完成后向主 WebView 注入**轮询探针 JS**，自动扫描主文档与同域 iframe 里的下载按钮 `href`；
  - 一旦发现 developer2 分发 URL，经 JsBridge 上报原生 → **立即自动关闭浏览器** → 应用内下载（`HttpURLConnection` 自动跟随 302 到 CDN）→ 完整性校验 → 安装，全程无需用户点击、无「网络异常/点下载没反应/卡页面」。
  - 原有「多窗口承接 + DownloadListener」保留作兜底；同时把 `developer2.lanrar.com/file/` 识别为可下载分发入口，手动点击下载按钮的路径同样能命中。
- 打开蓝奏页即自动开始解析并下载，成功后自动返回应用内继续安装。

---

## [3.1.7] - 2026-09-06

### 🐛 修复：蓝奏内置浏览器「点立即下载没反应 / 页面报网络异常 / 下载后浏览器不关闭、无任何提示」

- **根因**：v3.1.4 起蓝奏页改用「单 WebView + 关闭多窗口（`setSupportMultipleWindows(false)`）」承接下载。部分机型上蓝奏 `target=_blank` 的下载弹窗不再走应用的任何下载回调（既不经 URL 直链拦截也不触发 `DownloadListener`），而是被框架在应用数据目录里**静默下载** —— 表现为「点立即下载没反应」「应用数据增长但没有任何进度/安装提示」「浏览器窗口也不关闭」。
- **修复方案**：恢复**多窗口 + 隐藏子 WebView 承接 `target=_blank`**（v3.1.2 实测有效的下载捕获机制），并加固：
  - 主 WebView 与每个子 WebView 都挂「真实文件直链识别（`webgetstore`/`dmpdmp` 文件域、`.apk/.zip/.7z`）」+ `DownloadListener` 双捕获，任一视图命中真实下载立即回调；
  - 捕获后即刻关闭内置浏览器 → 转入应用内下载进度（可取消）→ 完整性校验 → 安装，下载/安装成败**必有明确提示**；
  - 命中即回调只执行一次（`AtomicBoolean` 去重），杜绝主/子视图重复触发。
- 页面「网络异常 / 点击无响应」同时消除：`window.open` 弹窗不再因关闭多窗口而失败，中间解析页 JS 在隐藏子 WebView 内正常解出 Cookie 与 CDN 直链。

### 🐛 修复：GitHub 下载偶发「网络错误」即失败

- GitHub 资产走多级重定向直链，国内网络偶发首连失败即报错。现下载失败后自动**短暂等待并重试一次**，减少一次性网络抖动导致的失败。

### ✨ 调整：「重新安装本版本」仅在已是最新版本时可用

- 入口改为**有条件的可用**：应用会静默获取 GitHub 最新版本信息，仅当「当前安装的版本已是最新」时该入口才可点击（点击直接用最新版信息弹出重装窗，不再重复请求网络）。
- 若存在更新版本，入口**置灰不可点**，副标题提示「当前可升级到 vX，请先用『检查更新』升级」，避免覆盖安装把新版本顶掉。
- 进入「关于」页会自动静默比对一次版本（不弹更新窗），保证可用状态即时准确。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.1.6] - 2026-09-06

### 🐛 修复：动态取色（Material You）界面不再跟随壁纸颜色

- **修复内容**：开启「动态取色」后界面配色恢复为**系统真实动态色板**（`dynamicLightColorScheme` / `dynamicDarkColorScheme`），更换壁纸时应用主色、容器色等会明显跟着壁纸变化，效果回到正常 Material You 观感。
- **根因**：v3.1.3 为统一界面与桌面图标的色相源，把动态取色从「系统动态色板」改成了「单一色相近似方案」（对基线 MD3 方案只替换一个色相）。基线中大量表面/容器色饱和度与明度很低，替换色相后视觉几乎不变 → 看起来就是「动态取色没效果」。
- **修复方案**：界面主题在动态取色开启时恢复使用系统动态色板（保留对 Android 12+ 与完整版的版本/分 flavor 判断）；桌面图标仍走「动态/自定义/预制统一色相源」映射最近预制色，二者互不干扰，开关与换壁纸时同步不变。
- 另附带：默认 `dynamic_theme=true` 时，未开启自定义/预制的用户也会随壁纸获得与系统一致的 Material You 配色。

### 🐛 修复：更新下载取消/关闭内置浏览器后残留临时文件与线程占用

- **取消下载 / 关闭蓝奏内置浏览器时**：立即取消下载协程、**断开正在阻塞的网络连接**（使卡在读超时上的下载线程立刻返回，不再悬挂占用后台线程），并马上删除 `cache/zlc_update` 下的临时下载文件（`download.bin` / `update.part` 等半成品）。
- 取消后不会再因为连接被中断而误弹「下载失败」错误提示（取消一律静默清理）。
- GitHub 下载与蓝奏下载共用该取消清理路径。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.1.5] - 2026-09-06

### ✨ 新增：「关于」页支持「重新安装本版本」（更新链路回归测试入口）

- 入口：设置 → 关于 → 应用 → **重新安装本版本**（位于「检查更新」下方）。
- 逻辑：点击后**不做版本号比对**，直接把 GitHub 最新 release（当前已是最新时＝当前版本）当作更新目标弹出安装弹窗，复用完整「下载 → 完整性校验 → 安装」链路。
- 用途：无需降级安装旧版本，即可随时验证应用内更新机制（蓝奏/GitHub 双渠道下载、安装器调用、新旧安卓安装适配）是否正常。
- 重装弹窗独立文案（标题「重新安装本版本」+ 覆盖安装说明），隐藏对同版本无意义的「跳过此版本」。
- v3.1.4 修复的「点下载跳浏览器」「安装后解析包出错」问题在本版本持续生效。

---

## [3.1.4] - 2026-09-06

### 🐛 修复：自动更新「点下载跳浏览器」+ 蓝奏/GitHub 双渠道安装均报「解析包出现问题」

- **修复点下载跳系统浏览器**：蓝奏下载是「分享页 → `target=_blank` 弹中间解析页 → JS 再跳 CDN 直链」两级结构，旧版用隐藏子 WebView 承接弹窗，部分机型下弹窗会逃逸到系统浏览器。现改为**单 WebView** 方案（`setSupportMultipleWindows=false`）：弹窗一律留在应用内加载，命中 CDN 文件直链立即转入应用内下载，其余链接绝不外跳。
- **修复双渠道安装「解析包出现问题」（长期根因）**：`startActivity` 拉起系统安装器后旧版**立即删除缓存 APK**，而系统安装器是**异步**经 FileProvider 读取文件的——文件已被删 → 安装器读不到完整包 → 一律报解析错误（蓝奏/GitHub 共用此路径，故双渠道必现）。现在成功拉起安装器后**不再删除**缓存文件（下次更新下载时自动清空，残留无害）。
- **下载完整性前置校验**：下载字节数与服务器 Content-Length 不一致（中途掐断/注入）直接判失败；安装前用 ZIP 中央目录校验安装包必须含 `AndroidManifest.xml` 与 `classes*.dex`，半成品/损坏包在应用内拦截并提示重试，不再喂给系统安装器。
- **安装器读取权限双保险**：Android 11+ 在 Manifest 用 `<queries>` 声明「查看 APK 安装包」意图（否则解析不到系统安装器）；拉起安装器前对解析到的安装器包名**显式授予** FileProvider URI 读权限，兜底部分国产 ROM 对一次性 flag 授权不敏感的问题。
- 兼容 Android 6（Go 版）~ Android 16（targetSdk 36）全代际安装链路。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.1.3] - 2026-09-06

### 🐛 修复：打开「动态取色」时桌面图标不更新

- **修复内容**：手动**打开**「Material You 动态取色」后退出到桌面，应用图标不跟随主题变色（仅「打开」这一下有此问题；「关闭」与换壁纸时正常）。
- **根因**：界面主题色与桌面图标共用一个色相源，但开启动态取色时壁纸色相是**下一帧异步**写入该色相源的，而自定义调色板/预制色是在设置函数里**同步**写入的；开关瞬间退出到桌面，`onStop` 的图标同步可能读到上一帧的旧值，图标便停在旧颜色。
- **修复方案**：动态取色改为与自定义取色同一链路——在开关处理函数内**同步**把壁纸色相写入共享色相源（启动时也同步一次），并移除原异步刷新逻辑。此后「打开动态取色 / 关闭动态取色 / 系统壁纸变色」三种变化，界面与桌面图标都基于同一最新色相，退出到桌面时图标必然跟随。
- 仅完整版生效，Go 轻量版不参与动态取色（图标保持默认，行为不变）。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.1.2] - 2026-09-06

### 🐛 修复：自动更新安装报「安装包解析出现问题」+ 内置浏览器体验修正

- **修复自动更新安装失败（解析包出错）**：蓝奏云反爬（`acw_sc__v2`）要求下载请求携带已解出的会话 Cookie，旧版在下载开始前就按 URL 后缀提前拦截并改用独立连接下载，导致服务端返回网页挑战页，损坏文件被交给系统安装器报「安装包解析出现问题」。
- **新下载策略**：不再按 URL 特征提前拦截，只在 WebView **真实开始下载文件**时才上报（`DownloadListener`）；`target=_blank` 的下载窗口由隐藏子 WebView 承接，确保反爬 JS 正常执行；下载请求复用 WebView 已解出的会话 Cookie；下载内容须为有效 ZIP/APK 才会进入解压/安装，非法内容直接提示重试而非触发安装器。
- **修复浏览器进入动画**：全屏窗口首帧即为满尺寸，页面内容从**右边缘整屏滑入**，不再从右下角带缩放弹出。
- **修复状态栏异常色块/条纹**：内容与顶栏背景延伸覆盖状态栏区域、底部提示延伸至手势条区域（`decorFitsSystemWindows=false` + inset 填充）。
- **移除右上角 × 按钮**：仅保留顶部返回箭头与系统返回键，均可关闭浏览器页面。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.1.1] - 2026-09-06

### 🐛 修复：动态取色下桌面图标不随系统壁纸颜色变化

- **修复内容**：开启「动态取色」（Material You）后修改系统壁纸颜色，应用界面能实时跟随，但**桌面图标颜色不更新**。
- **根因**：界面取色由 Compose 动态 ColorScheme 随系统主题实时刷新，而桌面图标依赖 7 个 `activity-alias` 切换，原实现仅在冷启动与退到后台（`onStop`）时同步一次；用户换壁纸期间应用一直处于后台，`onStop` 不会再次触发，图标便停留在旧颜色。
- **修复方案**：应用在后台时注册系统**壁纸颜色变化监听**（`WallpaperManager.OnColorsChangedListener`，Android 12+），壁纸/取色一变即在后台重新映射并切换桌面图标；前台期间仍不切换（避免别名切换导致 Activity 被系统中止闪退），该时段的壁纸变化由退出到桌面时的既有同步兜底。
- 仅完整版生效，Go 轻量版不参与动态取色（图标保持默认，行为不变）。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.1.0] - 2026-09-06

### 🆕 蓝奏云下载改「内置浏览器」方案（正式版）

- **彻底移除蓝奏云直链解析**：蓝奏云页面结构持续变动，程序直接解析直链（含反爬 `acw_sc__v2` 挑战、新版 kdns `/ajaxfile.php` 等）难以长期稳定，现全部移除，不再依赖任何页面结构猜测与公益 API。
- **内置浏览器打开下载页**：点击「从蓝奏云下载」后，在应用内置浏览器（WebView）中打开蓝奏云下载页，由你**在页面中自行点击下载按钮**。
- **拦截到下载即自动续流程**：检测到真实下载开始后，内置浏览器自动关闭并回到下载进度弹窗，下载完成后自动解压 APK 并调起安装（解压/权限续装/取消逻辑不变）。
- **返回与动画**：内置浏览器顶部提供返回/关闭按钮，**系统返回键同样绑定为关闭该页面**并回到「发现新版本」弹窗；进入页面带从右滑入动画，顶部进度条实时显示网页加载进度。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.5] - 2026-09-06

### 🆕 更新与下载体验重构（修复 v3.0.3/v3.0.4 实测问题）

- **修复「下载更新失败导致闪退」**：内部下载异常类继承关系有误（继承 `Throwable` 而非 `Exception`），UI 层捕获不到、异常逃逸到协程导致整个应用崩溃。现在下载失败会正常弹出错误提示，不再闪退。
- **蓝奏云恢复为内置下载并设为首选**：主按钮「从蓝奏云下载」直接在应用内解析分享页并下载（原生解析，不依赖任何公益 API，页面改版自动适配），无需跳浏览器；「从 GitHub 下载」移到次选位置，同样内置下载。
- **下载弹窗新增「取消」按钮**：点击立即中断下载并清理缓存中的半成品文件。
- **下载完成前不产生 `.apk` 半成品**：下载/解压全程使用 `download.bin`/`update.part` 临时名，完整就绪后才落成 `update.apk`，避免未完成文件被误识别/误安装。
- **安装权限授权后自动续装**：需要「安装未知应用」权限时引导去系统设置；授权返回后自动调起安装器（APK 已保留在缓存），无需重新下载。
- **启动自动清理下载缓存**：每次启动清理上次遗留的安装包与压缩包。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.4] - 2026-09-06

### 🧪 测试版

- 与 v3.0.3 功能内容完全一致（无代码改动），用于验证 3.0.3 更新弹窗（GitHub 内置下载 / 蓝奏云跳浏览器 / 更新说明 Markdown 排版）后的整体发布链路。

---

## [3.0.3] - 2026-09-06

### 🆕 更新与下载体验重构

- **修复「下载更新」无反应**：更新弹窗的弹窗渲染顺序有误——点「下载更新」后进度/失败提示都被「发现新版本」主弹窗提前拦截，界面看起来像没反应。现在下载进度、错误提示会正确覆盖显示。
- **默认渠道改为 GitHub**：主按钮「从 GitHub 下载」走应用内下载（自动匹配本版本对应安装包：标准版/Go 版按包分别匹配 `ZLC.apk` / `ZLC_Go.apk` 资产），带下载进度条，完成后调系统安装器。
- **新增「从蓝奏云下载」**：作为备用渠道，点击后**跳外部浏览器**打开蓝奏云分享页（不再内置解析/下载蓝奏云直链）。发布正文不含蓝奏云链接时该按钮自动隐藏。
- **更新说明支持 Markdown 排版**：新增轻量渲染器（无第三方依赖），支持标题、无序/有序列表（含嵌套）、加粗、行内代码、引用、分割线与 GitHub 表格；发布元数据行（`[蓝奏云-xx]: url`）仍自动隐藏。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.2] - 2026-09-06

### 🛠 修复与优化

- **拆解输出纯净静态照片（关键修复）**：拆解出来的照片现会**彻底抹除所有动态照片标记数据**，保证它再次被识别时是普通静态照片。
  - 原因：此前只清理第一个 XMP 段的部分标记，小米 MicroVideo、双 XMP 段、未知厂商命名空间等场景会在照片中残留 `MotionPhoto` / `MicroVideo` 字节，导致拆解照片被本应用再次误判成动态照片、无法作为封面用于「合成」；
  - 现在：遍历 JPEG **全部** XMP 段，以「任意命名空间前缀 + MotionPhoto/MicroVideo 开头」的规则移除属性、容器视频项与元素，清理后仍检测到残留字节时兜底删除全部 XMP，确保输出绝对干净；
  - 同时保留 Ultra-HDR（`hdrgm` / GainMap）元数据，拆解照片不损失 HDR 信息。
- **合成与拆解互斥**：队列含「合成」任务时不允许选择「拆解」输出模式（选项置灰并提示「队列含合成任务，不能选择拆解」，逻辑层同步拦截）；若处于拆解模式时导入合成素材，会自动切回 Google 输出，避免产出不可识别的产物。
- **文案精简**：输出格式「拆解（照片+视频）」更名为「拆解」。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.1] - 2026-09-05

### 🆕 新增功能

- **应用内更新下载与安装**：发现新版本后可直接在应用内下载并安装 APK，不再只跳转浏览器。
  - 双通道下载：GitHub Releases APK 直链 + 蓝奏云分享链接（从 Release 正文解析 `[蓝奏云-标准版]` / `[蓝奏云-Go版]` 两行，蓝奏压缩包自动解出其中 APK 后安装）；
  - 更新弹窗四个选项：「跳过此版本（记住版本号，同版本不再提示）/ 暂不更新 / 下载更新（默认蓝奏云，失败可切换 GitHub）/ 查看 GitHub」；
  - Go 版自动匹配带 Go 标识的安装包。

### 🛠 修复与优化

- **拆解保留 Ultra-HDR**：拆解时照片仅摘除动态照片（motion）相关 XMP 元数据，保留 HDR GainMap（`hdrgm`）等 Ultra-HDR 信息；视频含 HDR 元数据时原样字节复制，不做重编码。
- **合成失败策略调整（行为变更）**：移除合成素材自动重封装 / 转码流程（删除 FFmpeg 依赖，安装包体积减小）。合成遇非标准 MP4 容器（MOV / WebM / MKV / AVI 等）**直接判定该任务失败**，并提示先用其它工具转为标准 MP4（H.264/AAC），避免产出「可识别但无法播放 / 编辑」的损坏动态照片。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见下一条 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.0] - 2026-09-05

> ⚠️ **维护声明**：自本版本起，**停止维护与发布 Windows 桌面版**，Windows 源码保留于仓库（上一完整版本 v2.3.0）。此后仅发布 Android 版本。

### 🆕 新增功能

- **双版本体系**：
  - **ZLC（完整版）**：Android 10 (API 29)+，功能全量；
  - **ZLC Go（轻量版）**：Android 6 (API 23)+，独立包名可与完整版并存安装，为 Android 6–9 老设备精简（保留全部核心转换功能，移除 Material You 动态取色、触感反馈与按钮弹跳动画）。
- **合成动态照片**：选择 1 张封面照片 + 1 段不超过 3 秒的视频，一键合成为动态照片；导入时照片与视频分开计数、视频缩略图显示时长，合成输出按源照片拍摄时间命名并保留时间戳。
- **视频自动重封装 / 转码**：导入非标准 MP4 容器（WebM / MKV / AVI / MOV / TS 等）的素材时自动重封装或转码为标准 MP4（H.264 + AAC），并在封装前校验视频轨完整性，杜绝「只有音频、没有画面」的半成品。
- **拆解输出格式**：把动态照片一键拆解导出为「照片 + 视频」双文件。
- **魅族动态照片**：新增魅族动态照片格式识别与转换，转出时保留 MZCamera 私有元数据。
- **Material You 动态取色**：Android 12+ 可开启动态取色，界面颜色跟随系统壁纸色；关闭时可从 7 种预制主题色中选择，或使用自定义调色板自由取色相；主题切换带 400ms 平滑过渡动画。
- **动态桌面图标**：应用图标随当前主题色 / 壁纸色自动切换（7 套配色 + 深色/浅色双套资源），与主题色选择联动。
- **深色模式三选**：跟随系统 / 强制深色 / 强制浅色，切换即时生效。
- **检查更新**：启动时自动检查（可在设置中关闭）或设置页手动检查 GitHub 新版本，发现新版弹窗提示并一键跳转下载；Go 版自动匹配带 Go 后缀的安装包。
- **设置页全新改版**：外观（深浅色 / 动态取色 / 主题色）、默认选项、不再提示管理、关于等集中管理。
- **内置照片选择器改版**：相册改为垂直列表布局（展开方向为横向），「系统选择器」入口调整，「添加 / 批量导入」移入顶栏扩大图片展示区；扫描支持会话缓存，切换相册即中断重扫。

### 🛠 修复与优化

- 修复部分机型下动态照片转换后无法被相册识别 / 无法编辑播放的问题（详见「已知问题」）。
- 修复 Apple 动态照片（MOV 双文件）读入时重封装不彻底、残留 QuickTime 元数据轨导致的兼容性问题。
- 修复桌面图标在开启动态取色后不随壁纸主色变化的问题（壁纸主色偏灰时改用次色 / 第三色定色相）。
- 优化低版本 Android 的渲染与交互，保证 Go 版在 Android 6–9 流畅运行。
- 优化合成、重封装等长任务的处理反馈与取消逻辑。

### ⚠️ 已知问题

- **Apple 动态照片 → 其他格式存在兼容性 bug**：转换产物在 vivo 等相册中可被识别为动态照片、封面与 EXIF（位置 / 拍摄时间 / 机型等）均正常，但无法长按播放，也无法编辑（提示图片已破损）。原因为产物视频轨含 QuickTime 遗留的 HEVC / `mett` 元数据轨结构，与部分机型播放器 / 编辑器不兼容，正在修复中。
- **合成输入若为非标准 MP4 容器（如 WebM / MKV / AV1）可能出现 bug**：合成产物可能出现与上条相似的现象（可识别、可看封面，但无法播放 / 编辑）。应用会自动重封装或转码以规避，仍存在个别编码组合无法完美兼容的情况，建议合成时优先使用标准 MP4（H.264）素材。

---

## 历史版本

| 版本 | 说明 |
|------|------|
| v2.3.0 | Android + Windows 双端；vivo 单/双文件、小米 EXIF 0x8897、GPS 保留、列表持久化、批量导入（Windows 最终版） |
| v2.2.0 | 荣耀动态照片支持 |
| v2.1.0 | Windows 窗口动态排版与 DPI 感知最小窗口 |
| v2.0.0 | Windows + Android 双端重写 |
| v1.1.0 | 支持小米 / Apple Live Photo 格式 |
| v1.0.0 | 首个版本：Google / OPPO / vivo 格式互转 |
