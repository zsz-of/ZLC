# 更新日志 (Changelog)

本文件记录 Z-LivePhoto-Converter 各版本的更新内容。历史安装包请前往 [Releases](https://github.com/zsz-of/Z-LivePhoto-Converter/releases) 下载。

---

## [3.0.3] - 2026-09-06

### 🆕 更新与下载体验重构

- **修复「下载更新」无反应**：更新弹窗的弹窗渲染顺序有误——点「下载更新」后进度/失败提示都被「发现新版本」主弹窗提前拦截，界面看起来像没反应。现在下载进度、错误提示会正确覆盖显示。
- **默认渠道改为 GitHub**：主按钮「从 GitHub 下载」走应用内下载（自动匹配本版本对应安装包：标准版/Go 版按包分别匹配 `ZLC.apk` / `ZLC_Go.apk` 资产），带下载进度条，完成后调系统安装器。
- **新增「从蓝奏云下载」**：作为备用渠道，点击后**跳外部浏览器**打开蓝奏云分享页（不再内置解析/下载蓝奏云直链）。发布正文不含蓝奏云链接时该按钮自动隐藏。
- **更新说明支持 Markdown 排版**：新增轻量渲染器（无第三方依赖），支持标题、无序/有序列表（含嵌套）、加粗、行内代码、引用、分割线与 GitHub 表格；发布元数据行（`[蓝奏云-xx]: url`）仍自动隐藏。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.2] - 2026-09-06

### 🛠 修复与优化

- **拆解输出纯净静态照片（关键修复）**：拆解出来的照片现会**彻底抹除所有动态照片标记数据**，保证它再次被识别时是普通静态照片。
  - 原因：此前只清理第一个 XMP 段的部分标记，小米 MicroVideo、双 XMP 段、未知厂商命名空间等场景会在照片中残留 `MotionPhoto` / `MicroVideo` 字节，导致拆解照片被本应用再次误判成动态照片、无法作为封面用于「合成」；
  - 现在：遍历 JPEG **全部** XMP 段，以「任意命名空间前缀 + MotionPhoto/MicroVideo 开头」的规则移除属性、容器视频项与元素，清理后仍检测到残留字节时兜底删除全部 XMP，确保输出绝对干净；
  - 同时保留 Ultra-HDR（`hdrgm` / GainMap）元数据，拆解照片不损失 HDR 信息。
- **合成与拆解互斥**：队列含「合成」任务时不允许选择「拆解」输出模式（选项置灰并提示「队列含合成任务，不能选择拆解」，逻辑层同步拦截）；若处于拆解模式时导入合成素材，会自动切回 Google 输出，避免产出不可识别的产物。
- **文案精简**：输出格式「拆解（照片+视频）」更名为「拆解」。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.1] - 2026-09-05

### 🆕 新增功能

- **应用内更新下载与安装**：发现新版本后可直接在应用内下载并安装 APK，不再只跳转浏览器。
  - 双通道下载：GitHub Releases APK 直链 + 蓝奏云分享链接（从 Release 正文解析 `[蓝奏云-标准版]` / `[蓝奏云-Go版]` 两行，蓝奏压缩包自动解出其中 APK 后安装）；
  - 更新弹窗四个选项：「跳过此版本（记住版本号，同版本不再提示）/ 暂不更新 / 下载更新（默认蓝奏云，失败可切换 GitHub）/ 查看 GitHub」；
  - Go 版自动匹配带 Go 标识的安装包。

### 🛠 修复与优化

- **拆解保留 Ultra-HDR**：拆解时照片仅摘除动态照片（motion）相关 XMP 元数据，保留 HDR GainMap（`hdrgm`）等 Ultra-HDR 信息；视频含 HDR 元数据时原样字节复制，不做重编码。
- **合成失败策略调整（行为变更）**：移除合成素材自动重封装 / 转码流程（删除 FFmpeg 依赖，安装包体积减小）。合成遇非标准 MP4 容器（MOV / WebM / MKV / AVI 等）**直接判定该任务失败**，并提示先用其它工具转为标准 MP4（H.264/AAC），避免产出「可识别但无法播放 / 编辑」的损坏动态照片。

### ⚠️ 已知问题

- Apple 动态照片 → 其他格式的兼容性 bug 仍在修复中（详见下一条 v3.0.0 说明）。
- 合成需使用标准 MP4（H.264/AAC）素材；其他容器请先自行转码。

---

## [3.0.0] - 2026-09-05

> ⚠️ **维护声明**：自本版本起，**停止维护与发布 Windows 桌面版**，Windows 源码保留于仓库（上一完整版本 v2.3.0）。此后仅发布 Android 版本。

### 🆕 新增功能

- **双版本体系**：
  - **ZLC（完整版）**：Android 10 (API 29)+，功能全量；
  - **ZLC Go（轻量版）**：Android 6 (API 23)+，独立包名可与完整版并存安装，为 Android 6–9 老设备精简（保留全部核心转换功能，移除 Material You 动态取色、触感反馈与按钮弹跳动画）。
- **合成动态照片**：选择 1 张封面照片 + 1 段不超过 3 秒的视频，一键合成为动态照片；导入时照片与视频分开计数、视频缩略图显示时长，合成输出按源照片拍摄时间命名并保留时间戳。
- **视频自动重封装 / 转码**：导入非标准 MP4 容器（WebM / MKV / AVI / MOV / TS 等）的素材时自动重封装或转码为标准 MP4（H.264 + AAC），并在封装前校验视频轨完整性，杜绝「只有音频、没有画面」的半成品。
- **拆解输出格式**：把动态照片一键拆解导出为「照片 + 视频」双文件。
- **魅族动态照片**：新增魅族动态照片格式识别与转换，转出时保留 MZCamera 私有元数据。
- **Material You 动态取色**：Android 12+ 可开启动态取色，界面颜色跟随系统壁纸色；关闭时可从 7 种预制主题色中选择，或使用自定义调色板自由取色相；主题切换带 400ms 平滑过渡动画。
- **动态桌面图标**：应用图标随当前主题色 / 壁纸色自动切换（7 套配色 + 深色/浅色双套资源），与主题色选择联动。
- **深色模式三选**：跟随系统 / 强制深色 / 强制浅色，切换即时生效。
- **检查更新**：启动时自动检查（可在设置中关闭）或设置页手动检查 GitHub 新版本，发现新版弹窗提示并一键跳转下载；Go 版自动匹配带 Go 后缀的安装包。
- **设置页全新改版**：外观（深浅色 / 动态取色 / 主题色）、默认选项、不再提示管理、关于等集中管理。
- **内置照片选择器改版**：相册改为垂直列表布局（展开方向为横向），「系统选择器」入口调整，「添加 / 批量导入」移入顶栏扩大图片展示区；扫描支持会话缓存，切换相册即中断重扫。

### 🛠 修复与优化

- 修复部分机型下动态照片转换后无法被相册识别 / 无法编辑播放的问题（详见「已知问题」）。
- 修复 Apple 动态照片（MOV 双文件）读入时重封装不彻底、残留 QuickTime 元数据轨导致的兼容性问题。
- 修复桌面图标在开启动态取色后不随壁纸主色变化的问题（壁纸主色偏灰时改用次色 / 第三色定色相）。
- 优化低版本 Android 的渲染与交互，保证 Go 版在 Android 6–9 流畅运行。
- 优化合成、重封装等长任务的处理反馈与取消逻辑。

### ⚠️ 已知问题

- **Apple 动态照片 → 其他格式存在兼容性 bug**：转换产物在 vivo 等相册中可被识别为动态照片、封面与 EXIF（位置 / 拍摄时间 / 机型等）均正常，但无法长按播放，也无法编辑（提示图片已破损）。原因为产物视频轨含 QuickTime 遗留的 HEVC / `mett` 元数据轨结构，与部分机型播放器 / 编辑器不兼容，正在修复中。
- **合成输入若为非标准 MP4 容器（如 WebM / MKV / AV1）可能出现 bug**：合成产物可能出现与上条相似的现象（可识别、可看封面，但无法播放 / 编辑）。应用会自动重封装或转码以规避，仍存在个别编码组合无法完美兼容的情况，建议合成时优先使用标准 MP4（H.264）素材。

---

## 历史版本

| 版本 | 说明 |
|------|------|
| v2.3.0 | Android + Windows 双端；vivo 单/双文件、小米 EXIF 0x8897、GPS 保留、列表持久化、批量导入（Windows 最终版） |
| v2.2.0 | 荣耀动态照片支持 |
| v2.1.0 | Windows 窗口动态排版与 DPI 感知最小窗口 |
| v2.0.0 | Windows + Android 双端重写 |
| v1.1.0 | 支持小米 / Apple Live Photo 格式 |
| v1.0.0 | 首个版本：Google / OPPO / vivo 格式互转 |
